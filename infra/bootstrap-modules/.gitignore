# Compiled TypeScript output — regenerated by `npm run build` before every CDK deploy.
# Do not commit: module-runner.cjs reads from dist/ at runtime but the pre-commit
# hook and module-runner both run `npm run build` to keep it fresh.
dist/
*.js.map
*.d.ts
